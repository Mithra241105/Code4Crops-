# Krishi-Route Frontend Environment Variables - PLACEHOLDER TEMPLATE

# 1. API URL (Required)
# The URL of your running Krishi-Route backend
VITE_API_URL=http://localhost:5000/api

# 2. Google Maps API Key (Highly Recommended)
# Required for full Google Maps features (Live tracking, dynamic routing)
# If left blank, the app falls back to OpenStreetMap (Leaflet)
VITE_GOOGLE_MAPS_KEY=replace_with_your_google_maps_api_key
